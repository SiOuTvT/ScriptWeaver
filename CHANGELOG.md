# ScriptWeaver 版本改动台账

本文件是版本发行说明的唯一数据来源。每完成一项功能改动，务必在此登记对应版本号下；写发行说明时直接照抄本台账，禁止凭记忆编写，禁止遗漏条目。

历史版本记录：v0.1.0 至 v1.0.0 的说明见各版本 release 文件夹中的发行说明（release/0.x.0/、release/1.0.0/），不在此重复登记。

---

## v1.2.0（开发中，未封板）

**状态**：v1.1.0 已于 2026-08-04 封板并归档到 release/1.1.0/。以下条目均为 v1.2.0 已完成的改动，按完成时间倒序登记。

### 主题与显示
- 默认主题改为浅色，首次启动不再闪白、不再默认深色。
- 修复浅色深色切换后重启、刷新时主题状态错乱的问题，重启后保持上次选择。
- 修复顶部主题按钮图标与实际主题相反的问题：浅色模式显示太阳、深色模式显示月亮。

### AI 接入
- 新增 AI 厂商预设：OpenAI、DeepSeek、OpenRouter、千问、智谱，另保留自定义厂商入口。
- 新增「拉取模型列表」按钮，配置厂商后一键拉取该厂商可用对话模型，无需手填。
- 内置三种模式的固定提示词：舞台监督、文学导师、剧情蓝图，切换模式即用对应提示词。

### Ren'Py 引擎识别
- 新增手动指定 Ren'Py SDK 路径（带浏览按钮），并自动补充常见安装位置探测，解决「明明装了却检测不到」的问题。
- 自动探测扩展到所有磁盘根目录（如 D:\renpy-8.5.2-sdk 也能被发现），不再局限于 C 盘与下载目录。

### 备份机制
- 关闭窗口改为最小化到系统托盘，不直接退出程序。
- 退出程序时自动做一次备份，防止改动丢失。
- 备份界面改为手动备份，由用户自主决定何时存档；内容未变化时不再重复建档。
- 保留自动安全网备份，但间隔放宽到 30 分钟，不再频繁打扰。

### 协作功能
- 修复协作功能因网络限制完全无法建立主机的问题。
- 支持自定义信令服务器地址，默认服务不可用时可以换自己的服务器。
- 协作双方现在会实时同步素材库、角色信息与变量改动，不只是剧本文本。

### 性能优化
- 时间轴滚动与长剧本渲染大幅提速，拖动不卡顿。
- 图片素材读取增加缓存，重复显示不再反复读取文件。
- 自动保存间隔、撤销步数等参数按更合理的默认值调整。

### 全站体检落地
- 语法学院补全动态背景课程，并核对官方语法细节。
- 特效库与帮助中心新增「复制代码」「插入到剧本」按钮，示例可直接用于自己的剧本。
- 素材管理、角色管理等页面补齐拖拽与试听交互。

### 导入修复
- 修复重复导入工程后舞台立绘背景不显示的问题：重复导入同一工程时，素材 id 引用可能失效（剧本指向不存在的素材），现已保证每次导入的引用都指向真实存在的素材，并补充回归测试。
- Ren'Py 引擎自动探测扩展到所有磁盘根目录。

### 导出修复
- 修复导出的剧本在 Ren'Py 中无法运行的问题（实测 lint 报错已全部修复）：
  - 角色占位名（如 ???）清洗为合法标识符，不再生成 define ??? 与 ??? "台词"。
  - 位置槽位名（如 left-center）清洗为下划线（left_center），transform 定义与 show at 引用同步闭合。
  - 视频背景改用官方合法的 scene expression Movie(...) 写法，不再输出非法的 scene bg = Movie(...)。
  - 跳过设置改用官方正确的偏好写法 _preferences.skip_unseen / _preferences.skip_after_choices（原 config.skip_unseen 与误改的 config.skip_unseen_text 均非法，已对照 SDK 源码逐一核对全部配置变量）。
  - 修复入口 label 重复定义：剧本自带 label start 时不再额外发射，消除「label defined twice」报错。
  - 新增 6 个回归测试；用真实工程跑官方 Ren'Py lint 验收通过（exit 0）。

### 试玩前自动校验
- 点「试玩」「打包成安装包」时自动先跑一遍 Ren'Py 官方语法校验，语法有问题会直接把错误列出来并停止启动，不会再出现「启动窗口才报错」的情况。

### 数据目录（默认不存 C 盘）
- 应用一切数据（缓存、版本快照、AI 配置、测试运行暂存、构建日志等）默认全部保存到 D 盘，绝不默认写入 C 盘（不再使用 AppData\Roaming）。
- 默认数据目录为 D:\ScriptWeaverData，可通过环境变量 SW_DATA_DIR 自定义到任意位置。
- 设置中心「数据与缓存」新增「存储位置」展示，直观显示当前数据目录。

---

## 待办提示
- 每次完成新功能或修复，在此文件对应版本号下新增一条描述（面向用户，不要写内部技术名词、文件路径）。
- 封板前核对本台账与 git 提交记录，确保无遗漏。
