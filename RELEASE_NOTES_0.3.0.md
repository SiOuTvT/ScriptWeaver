# ScriptWeaver v0.3.0 发行说明

**发布日期**：2026-07-20
**适用升级**：从 v0.2.0 升级
**涵盖范围**：v0.2.0 至今的全部新增、改进与修复

---

## ✨ 新增功能

### 1. 特效大本营（Effects Lab）
全新特效浏览与实时预览引擎，可在软件内直接预览 Ren'Py 特效效果并对照作者指南。
- 预览引擎：基于 Web 动画渲染过渡 / 变换 / warpers / 描边闪烁等效果，支持时长 / 幅度参数化。
- 预览舞台：单精灵元素 + 背景 / 精灵资源选择（本地上传）、视频比例布局、对话层 / 闪光 / 球 / 字幕分层。
- 特效百科：15 个专题（基础 / atl / builtin / crop / impact / movement / stage3d / tfcolor / tfcrop / tfpan / tfpos / tfrot / warpers / zoom），每条含作者指南、参数手册、CSS 实现、性能提示与参数表。
- 导航与入口：左侧目录导航 + 大纲平滑跳转，预览入口统一收归左侧栏。

### 2. 系统托盘
- 关闭窗口时最小化到系统托盘而非退出；仅经托盘菜单「退出」真正终止进程。
- 内置托盘图标兜底生成，无外部图标文件也能正常显示。

### 3. 舞台预设槽位 角色缩放 逐行音频偏移 Auto 自动播放
- 预设位置槽：新增左中 / 右中等预设槽位，拖拽吸附对齐；支持应用预设、复制上一角色位置、锁定轴。
- 每角色缩放：角色支持独立缩放，并反映到 Ren'Py 导出的 show 语句。
- 逐行音频偏移：SE / 语音支持行内拖拽偏移，新增 Alt+方向键（±50ms）、Alt+Shift+方向键（±250ms）微调。
- Auto 自动播放：按偏移依次播放 BGM / 环境 / SE / 语音用于审阅。

### 4. 画布比例持久化
- 项目级画布比例持久化，加载时恢复；新增 Ren'Py 风格比例预设选择器（默认 16:9），自适应 letterbox 缩放。

---

## 🔧 改进

### 舞台预览大幅优化
- 角色拖拽边界修正：精灵始终完整可见、不溢出裁剪；加载时归一化越界坐标。
- 不再缩小：靠近边缘移动时不再视觉缩小。
- 背景整图显示：背景图按原始比例居中整图显示不裁切，letterbox 区域用背景主色调填充。
- 拖拽体验：拖拽中角色置顶避免重叠；拖拽更跟手、不再卡死。
- 布局稳定：舞台布局更稳固，不再塌陷。
- 编辑器侧栏：角色 / standee 编辑器改为固定右侧，不再遮挡舞台。
- 素材管理页缩略图：精灵卡片改为竖版、透明背景、顶部对齐，角色更清晰；背景维持小方块。

### 素材管理页（Assets）音频区重构
- 两侧音频区统一卡片设计：圆形播放 / 暂停按钮 + 类别徽标（BGM / 环境 / 音效 / 语音）+ 文件名；播放中蓝边高亮，点击按钮即可试听。
- 图片 / 音频卡片现在可直接拖到舞台预览。

### 按钮体系收敛
- 主按钮背景更柔和，整体色调更统一。
- 字号体系收敛，视觉更统一。

### 其他
- 特效百科文案与 Ren'Py 信息保持一致，描述更清晰。

---

## 🐛 修复
- 修复左下角版本号显示错误：此前 v0.2.0 安装包误显 v0.1.0，本版起正确显示 **v0.3.0**。
- 修复导入素材的音频在软件内无法播放的问题。
- 修复重启 / 热重载后导入的图片、音频素材丢失的问题，现在可跨重启保留。
- 修复角色拖拽时卡死 / 边界错位，以及编辑器侧栏遮挡舞台的问题。
- 修复舞台布局塌陷与精灵缩略图对齐问题。
- 修复特效大本营（Effects Lab）打开时报错无法加载的问题。

---

## ⚠️ 关于 v0.2.0
v0.2.0 安装包左下角仍会显示 **0.1.0**（仅显示错误，功能完全正常），已重新打包修正版并替换 GitHub 附件，下载即正确显示 **v0.2.0**。

---

## 📦 安装包
- ScriptWeaver-0.3.0-x64.exe（标准安装包）
- ScriptWeaver-0.3.0-x64.zip（免安装绿色版）
- 自动更新索引 latest.yml + .blockmap
