# 全站深度体检报告

体检范围：语法学院、特效/生态组件库、导入→编辑→保存→导出全链路、全站按钮与死角、响应式布局。
依据 Ren'Py 官方 8.x 规范逐项核对。

---

## 一、语法学院（41 课）覆盖核查

### 结论：用户点名的 11 项主题全部覆盖

| 要求主题 | 对应课程 | 状态 |
|---|---|---|
| ATL 复杂路径动画 | atl-paths | 已覆盖，含多点路径+循环范例 |
| Transform 变换 | atl-basics + 内置定位变换 | 已覆盖 |
| NVL 模式 | nvl-mode / nvl-advanced | 已覆盖 |
| 自定义 Screen 界面 | custom-screens + screen-actions/timer/input/use | 已覆盖 |
| Python 内联逻辑 | python-blocks | 已覆盖 |
| Save/Load 与持久化 | save-load / persistent-data | 已覆盖 |
| 多语言本地化 (tl) | translation | 已覆盖，含 tl 目录与 _() 标记 |
| Input 表单输入 | screen-input | 已覆盖 |
| 无障碍 | accessibility | 已覆盖，含 self-voicing 范例 |
| Voice/Audio 通道 | play-statements | 已覆盖 |
| Movie 动态背景 | video-system | **已补全**（见下） |

### 本次补全
- **video-system（视频播放）**：原内容只讲 `renpy.movie_cutscene` 过场，对「Movie 动态背景」讲得太薄。已补全：新增动态背景用法（`Movie(play=..., loop=True)` 挂背景层循环播放）、视频格式建议、文件命名约定、loop=False 单次播放、打包体积提示，并给出可运行的完整范例（雨夜窗景动态背景 + 过场动画）。

其余 40 课抽查（atl-paths、translation、accessibility、nvl、screen、audio 等）均为「功能作用 + 通俗解释 + 避坑提示 + 可运行范例」四段式完整内容，无空洞占位。

---

## 二、特效大本营 & 生态组件库

### 发现的问题：特效百科「只摆不产」——0 个复制/插入功能
- 特效百科 14 大分类（基础转场 / CropMove / 位移 / 缩放镜头 / 冲击抖动 / 变换属性系列 / 缓动 / ATL / 内置定位 / 3D 舞台）内容极全，每项含中文名、语法、原理、参数手册、性能避坑。
- 但详情页只有「进入预览舞台」，**没有一键复制或插入**，等于摆设。

### 已修复
1. **特效百科详情页**：Ren'Py 代码示例板块新增两个真实按钮：
   - 「复制 .rpy 代码」：一键把该特效的完整 Ren'Py 代码（含注释头）复制到剪贴板，成功有提示。
   - 「插入到剧本」：把代码插入当前项目末尾并跳回场景导航，有成功提示。
2. **Ren'Py 生态大厅（语法课 + 社区插件）**：原来只有「插入到剧本」，补上了「复制代码」按钮（修复了 Copy 图标导入却从未使用的问题）。

---

## 三、导入 → 编辑 → 保存 → 导出 闭环实测

用真实工程 lulu 跑完整闭环（真实文件系统驱动）：

| 环节 | 结果 |
|---|---|
| 导入解析 | 66 个 delta 解析成功，背景/立绘/音频正确匹配 |
| 编辑 | 修改一句台词为「闭环测试」 |
| 保存 | 全量序列化再还原（structuredClone 等价），**无损** |
| 导出 | reduceLines + buildBundle 生成 255 行 .rpy，**修改后的台词正确写入**，含 label start，无断头 |

**结论：导入→编辑→保存→导出链路无断点、无数据丢失、无静默失败。**（此前修复的音频绑定、视频背景识别、图片匹配在此链路中一并验证有效。）

---

## 四、全站按钮与死角扫描

- 设置中心：4 个 Tab（通用/外观/AI 配置/数据与缓存）全部有实质内容，无空 Tab。
- 版本历史：创建快照 / 恢复 / 删除全部绑定处理器 + 加载中禁用态。
- 全站 grep：无空 onClick、无 `onClick={() => {}}` 死按钮；「暂未开放」仅出现在素材管理里视频/特效预设两个未开放类别的优雅空态（有意为之）；图片加载失败均有色块占位而非空白。

---

## 五、遗留项修复（第二轮已完成）

1. **时间轴虚拟化 ✅**
   - 千行级大工程下，时间轴原本把每一行都渲染成 DOM（几千个单元格）。
   - 已为轨道 DropCell 与行号单元格启用 CSS 原生虚拟化（content-visibility: auto + contain-intrinsic-size）：视口外的单元格直接跳过布局与绘制，滚动到时再渲染。
   - 该方案不改动任何数据逻辑、不破坏绝对定位的音频色块/角色片段层，风险最低；导入千行工程后时间轴滚动流畅度应显著提升。
2. **响应式深度验证 ✅（代码级）**
   - 逐页检查固定宽度容器：各页面均为「固定宽侧栏 + flex-1 主区」结构，无会造成右侧大面积留白的整页 max-w 容器。
   - max-w 仅用于弹窗（命令面板、导入弹窗）与文章阅读列（特效百科、角色详情），属有意的可读性设计。
   - 未发现固定宽度溢出的卡片或异常滚动条源头。多分辨率真机截图验证仍建议你随手抽查，如有具体窗口尺寸下的错位，报给我再针对性修。

---

## 验证记录

- tsc --noEmit 0 错误；lint 0 错误。
- rpyImporter + rpyExporter + reducer 测试全过。
- 闭环真实测试 PASS（66 delta → 255 行导出）。
- Electron 窗口已重启加载全部改动（含时间轴虚拟化）。
